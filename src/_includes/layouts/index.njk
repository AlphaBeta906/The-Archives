<!DOCTYPE html>
<html lang="en">

<head>
    <title>{{ title }}</title>
	<link rel="stylesheet" href="{{ '/assets/css/index.css' | url }}">
</head>
<body style="padding: 1rem;">
	<div style="display: flex; flex-direction: column; gap: 1rem;">
		<div>
			<h1 style="margin: 0;">{{ title }}</h1>
			{% if special %}
				<small>Special page</small>
			{% elif moc %}
				<small>Map of content</small>
			{% elif help %}
				<small>Help page</small>
			{% else %}
				<small>Article page</small>
			{% endif %}
		</div>
		<div>
			<a href="/">The Index</a> - <a href="/about">About Us</a> - <a href="/objectives">Our Objectives</a> - <a href="/contact">Contact Us</a>
		</div>
	</div>

	<hr style="margin-top: 0.5rem; margin-bottom: 0.5rem;">
	<div style="margin-bottom: 1rem; display: flex; width: 100%; height: 1.5rem; align-items: center; justify-content: space-between;">
		<small>Provided by The Archives</small> 
		<div>
			{% if featured %}
				<img src="https://upload.wikimedia.org/wikipedia/commons/b/bc/Featured_article_star.svg" style="width: 1.5rem; height: auto;">
			{% endif %}
		</div>
	</div>

	{{ content | safe }}
</body>